function onEvent(eventName, value1, value2)
    if name == 'a camera zoom event' then -- it seemed you forgot this

        setProperty('defaultCamZoom', value1)
        
        debugPrint(value2)
        
        end
        
    
end